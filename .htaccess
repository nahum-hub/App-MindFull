<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Bloquear acceso a carpetas sensibles
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
